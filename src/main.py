import subprocess
import os
import sys

def run_terminal():
    # command_handler = CommandHandler() # Use this once implemented
    command_handler = PlaceholderCommandHandler() # Using placeholder for now
    
    while True:
        try:
            #load command history, load config, load the prompot and AI?
        
def main():
    

if __name__ == "__main__":
    main()
